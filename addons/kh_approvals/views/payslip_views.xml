<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_kh_approval_request_list" model="ir.ui.view">
        <field name="name">kh.approval.request.list</field>
        <field name="model">kh.approval.request</field>
        <field name="arch" type="xml">
            <list string="Approval Requests">
                <field name="name"/>
                <field name="requester_id"/>
                <field name="date_from"/>
                <field name="date_to"/>
                <field name="state" widget="badge" 
                       decoration-success="state == 'approved'" 
                       decoration-info="state == 'submitted'" 
                       decoration-muted="state == 'draft'"/>
            </list>
        </field>
    </record>

    <record id="view_kh_approval_request_form" model="ir.ui.view">
        <field name="name">kh.approval.request.form</field>
        <field name="model">kh.approval.request</field>
        <field name="arch" type="xml">
            <form string="Approval Request">
                <header>
                    <button name="action_submit" string="Submit" type="object" class="oe_highlight" 
                            invisible="state != 'draft'"/>
                    
                    <button name="action_approve" string="Approve" type="object" class="oe_highlight" 
                            invisible="state != 'submitted'"/>
                    
                    <button name="action_reject" string="Reject" type="object" 
                            invisible="state != 'submitted'"/>
                            
                    <button name="action_draft" string="Reset to Draft" type="object" 
                            invisible="state == 'draft'"/>
                            
                    <field name="state" widget="statusbar" statusbar_visible="draft,submitted,approved"/>
                </header>
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" placeholder="e.g. Vacation Request"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="requester_id"/>
                            <field name="approver_id"/>
                        </group>
                        <group>
                            <field name="date_from"/>
                            <field name="date_to"/>
                        </group>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="action_kh_approval_request" model="ir.actions.act_window">
        <field name="name">Approval Requests</field>
        <field name="res_model">kh.approval.request</field>
        <field name="view_mode">list,form</field>
    </record>

    <menuitem id="menu_kh_approval_root" name="Approvals" sequence="10"/>
    <menuitem id="menu_kh_approval_request" name="Requests" parent="menu_kh_approval_root" action="action_kh_approval_request"/>

</odoo>
