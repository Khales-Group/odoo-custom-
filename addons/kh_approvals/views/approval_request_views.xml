<odoo>
  <data>

    <!-- REQUEST LIST -->
    <record id="view_kh_approval_request_list" model="ir.ui.view">
      <field name="name">kh.approval.request.list</field>
      <field name="model">kh.approval.request</field>
      <field name="arch" type="xml">
        <list>
          <field name="name"/>
          <field name="requester_id"/>
          <field name="amount"/>
          <field name="currency_id"/>
          <field name="state"/>
        </list>
      </field>
    </record>

    <!-- REQUEST FORM -->
  <record id="view_kh_approval_request_form" model="ir.ui.view">
  <field name="name">kh.approval.request.form</field>
  <field name="model">kh.approval.request</field>
  <field name="arch" type="xml">
    <form string="Approval Request">
      <header>
  <!-- Shows only in Draft -->
  <button name="action_submit"
          type="object"
          string="Submit"
          class="oe_highlight"
          states="draft"/>

  <!-- (optional) buttons you may want later -->
  <!--
  <button name="action_reset_to_draft" type="object" string="Reset to Draft" states="in_review,approved,rejected"/>
  <button name="action_cancel"         type="object" string="Reject"        states="in_review"/>
  <button name="action_force_approve"  type="object" string="Approve"       states="in_review"/>
  -->

  <field name="state" widget="statusbar"
         statusbar_visible="draft,in_review,approved,rejected"/>
      </header>

      <sheet>
        <group>
          <group>
            <field name="name" required="1"/>
            <field name="requester_id" readonly="1"/>
            <field name="amount"/>
            <field name="currency_id"/>
            <field name="line_rule_ids" widget="many2many_tags" string="Approval Route"/>
          </group>
        </group>

        <notebook>
          <page string="Approval Steps">
<field name="approval_line_ids">
  <list editable="bottom">
                <field name="name"/>
                <field name="approver_id"/>
                <field name="required"/>
                <field name="state"/>
                <field name="note"/>
              </list>
            </field>
          </page>
        </notebook>
      </sheet>

      <!-- ✅ v18 chatter: use <chatter>, not <div> or a notebook page -->
      <chatter>
        <field name="message_follower_ids"/>
        <field name="activity_ids"/>
        <field name="message_ids"/>
      </chatter>
    </form>
  </field>
</record>
  </data>
</odoo>
