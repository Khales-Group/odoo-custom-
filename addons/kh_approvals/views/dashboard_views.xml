<odoo>
  <data>
    <!-- Dashboard Action -->
    <record id="action_kh_approval_dashboard" model="ir.actions.client">
      <field name="name">Dashboard</field>
      <field name="tag">kh_approvals.approvals_dashboard</field>
    </record>

    <!-- Dashboard Template -->
    <template id="kh_approvals.dashboard_template" name="Approval Dashboard">
      <div class="container o_kanban_view">
        <div class="row">
          <t t-foreach="rules" t-as="rule">
            <div class="col-md-3">
              <div class="o_kanban_record">
                <div class="o_kanban_card_header">
                  <div class="o_kanban_card_header_title">
                    <div class="o_primary"><t t-esc="rule.name"/></div>
                  </div>
                </div>
                <div class="o_kanban_card_content">
                  <button class="btn btn-primary" name="new_request" type="button">New Request</button>
                </div>
              </div>
            </div>
          </t>
        </div>
      </div>
    </template>
  </data>
</odoo>